# Hoist `next` to the top-level node_modules so Turbopack's internal
# resolver can find it. Without this, pnpm's strict symlink structure
# causes a "Next.js package not found" panic in Turbopack's HMR loop.
public-hoist-pattern[]=next
public-hoist-pattern[]=next-*
