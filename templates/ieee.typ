// Local IEEE-like Typst template note
//
// Phase 6 originally planned to vendor the charged-ieee template. For this MVP,
// the server route builds a self-contained two-column Typst document in
// src/lib/export/typst-template.ts so PDF export works offline and never fetches
// templates at runtime. Replace that helper with a pinned charged-ieee template
// later if exact journal formatting becomes necessary.
