// Pipeline execution page — runs ARS agents and streams output section by section.
// Built out in Phase 4.

export default function PipelinePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-bold mb-2">Pipeline</h1>
      <p className="text-muted-foreground">Pipeline execution UI — coming in Phase 4.</p>
    </main>
  )
}
